# CAEP Data Project for Endel Associates

## Interactive Dashboard

Run the Plotly Dash app with:

```bash
pip install -r requirements.txt
python dashboard.py
```

Notes abt building documentation:
need some kind of crosswalks (a la types.json, similar) between database values and readable values
need to have some DOCUMENTED way of describing the demographics fields--eventually will need this to extend to more complex descriptions that are not just in one table!! i think dbt is inevitable. 

-TODO:
- check on sql injection!!